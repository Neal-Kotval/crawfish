---
phase: 20-cloud-issue-ingestion-linear-github-connectors-with-postgres
plan: 04
type: execute
wave: 4
depends_on: ["20-02", "20-03"]
files_modified:
  - cloud/platform/src/lib/api.ts
  - cloud/platform/src/pages/Connections.tsx
  - cloud/platform/src/pages/ProjectIssues.tsx
  - cloud/platform/src/pages/OrgRoute.tsx
autonomous: false
requirements: [REQ-orch-issue-intake, REQ-knowledge-connectors]

must_haves:
  truths:
    - "A user sees a connections panel showing GitHub and Linear connected/disconnected state"
    - "Clicking Connect Linear sends the user to the Linear authorize URL returned by the server"
    - "A user opens a project's issues view and sees synced issues with state and labels"
    - "A Sync now control triggers ingestion and the issue list/count updates"
    - "For Linear, the user picks a Team for the project before sync is enabled"
  artifacts:
    - path: "cloud/platform/src/lib/api.ts"
      provides: "listIntegrations, connectProvider, listIssues, syncProject, selectLinearTeam helpers + types"
      contains: "listIssues"
    - path: "cloud/platform/src/pages/Connections.tsx"
      provides: "per-provider connect/disconnect panel"
      contains: "Connections"
    - path: "cloud/platform/src/pages/ProjectIssues.tsx"
      provides: "issues list + Sync now + Linear Team picker"
      contains: "ProjectIssues"
  key_links:
    - from: "cloud/platform/src/pages/ProjectIssues.tsx"
      to: "GET/POST /api/orgs/:id/projects/:pid/{issues,sync}"
      via: "listIssues/syncProject in api.ts"
      pattern: "listIssues\\(|syncProject\\("
    - from: "cloud/platform/src/pages/Connections.tsx"
      to: "POST /api/orgs/:id/integrations/linear/connect"
      via: "connectProvider → window redirect to authorizeUrl"
      pattern: "authorizeUrl"
    - from: "cloud/platform/src/pages/OrgRoute.tsx"
      to: "Connections + ProjectIssues"
      via: "tab dispatch (REGISTRY — serialize)"
      pattern: "ProjectIssues|Connections"
---

<objective>
Ship the `cloud/platform` UI: typed API helpers, a Connections panel (per-provider connected/disconnected + Connect Linear redirect), and a Project Issues view (issue list with state/labels, a "Sync now" control, and a Linear Team picker that must be set before Linear sync is enabled). Wire both into OrgRoute's tab dispatch. A human-verify checkpoint confirms the rendered flow via the DEV-AUTH bypass.

Purpose: completes the user-facing half of success criteria 4 and 5 — issues render with state/labels and round-trip from the provider; "Sync now" reflects updated counts. Closes REQ-orch-issue-intake's UI surface and REQ-knowledge-connectors' connect UX.
Output: extended api.ts, two new pages, OrgRoute wiring, and a verified UI walk.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-cloud-issue-ingestion-linear-github-connectors-with-postgres/20-CONTEXT.md
@.planning/phases/20-cloud-issue-ingestion-linear-github-connectors-with-postgres/20-RESEARCH.md
@.planning/phases/20-cloud-issue-ingestion-linear-github-connectors-with-postgres/20-02-SUMMARY.md
@.planning/phases/20-cloud-issue-ingestion-linear-github-connectors-with-postgres/20-03-SUMMARY.md
@cloud/platform/DEV-AUTH.md

<interfaces>
<!-- Existing platform conventions the executor must reuse. Extracted from the codebase. -->

From cloud/platform/src/lib/api.ts (extend — match apiFetch + unwrap<T> + typed-helper style):
- `apiFetch(path, init)` attaches dev (X-User-Id from localStorage.cf_dev_user) or Clerk Bearer headers; use it for every call.
- `unwrap<T>(res)` parses { error: { code, message } } on failure into an ApiError with `.code`/`.status`.
- existing helper shape: `export async function listProjects(orgId): Promise<ProjectSummary[]>` calling `apiFetch(\`/api/orgs/${encodeURIComponent(orgId)}/projects\`)`.

Server routes available (Waves 2–3):
- GET  /api/orgs/:id/integrations → [{ provider, connected, externalWorkspaceName }]
- POST /api/orgs/:id/integrations/linear/connect → { authorizeUrl }
- POST /api/orgs/:id/integrations/linear/select-team (body { projectId, teamId, teamKey })
- GET  /api/orgs/:id/projects/:pid/issues → Issue[] (provider, externalKey, title, state, url, labels(JSON string), assigneeExternal, externalUpdatedAt, syncedAt)
- POST /api/orgs/:id/projects/:pid/sync → { provider, upserted, count }
- (Linear teams for the picker: server exposes them via the connect/select-team flow — confirm in 20-03-SUMMARY whether a GET teams route exists; if not, surface the picker from data the select-team route validates, and document any gap.)

NOTE: `labels` arrives as a JSON-encoded string (sqlite convention) — `JSON.parse(labels)` on the client to render chips. This is the documented read-side contract (RESEARCH Pitfall 5).

From cloud/platform/src/pages/Projects.tsx + OrgRoute.tsx (conventions to copy):
- Import `Eyebrow`, `Pill` from `@crawfish/ui/components/*`; `formatApiError` from `@crawfish/ui/lib/formatApiError`.
- LoadState discriminated-union pattern ({ kind: "loading" | "disconnected" | "error" | "ok" }).
- CSS lives ONLY in ui/tokens/globals.css (CLAUDE.md) — use existing tokens (var(--ink), var(--rule-3), var(--surface-2), cfp-btn cfp-btn--primary). Do NOT create a component-scoped CSS file.
- OrgRoute.tsx is a REGISTRY route table (CLAUDE.md) — serialize the edit. Existing tabs: canvas, projects, board, sessions, knowledge, diagnoses, team, billing, settings. "settings" body already names "integrations" — surface Connections under a tab (e.g. reuse "settings" or add a connections surface) and ProjectIssues under the project view; decide and document.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add API helpers + Connections panel + ProjectIssues view, wire into OrgRoute</name>
  <read_first>
    - cloud/platform/src/lib/api.ts (extend — apiFetch/unwrap/typed-helper pattern)
    - cloud/platform/src/pages/Projects.tsx (LoadState union, Eyebrow/Pill, fetch + error handling, token usage)
    - cloud/platform/src/pages/OrgRoute.tsx (REGISTRY tab dispatch — serialize the edit)
    - cloud/platform/DEV-AUTH.md (dev bypass for verification)
  </read_first>
  <action>
    Extend `cloud/platform/src/lib/api.ts`: add types `Integration = { provider: "github"|"linear"; connected: boolean; externalWorkspaceName: string | null }` and `Issue = { id; provider; externalKey; title; state: "open"|"closed"; url: string|null; labels: string; assigneeExternal: string|null; externalUpdatedAt: string|null; syncedAt: string }`, and helpers `listIntegrations(orgId)`, `connectProvider(orgId, "linear"): Promise<{ authorizeUrl: string }>`, `selectLinearTeam(orgId, { projectId, teamId, teamKey })`, `listIssues(orgId, pid): Promise<Issue[]>`, `syncProject(orgId, pid): Promise<{ provider; upserted; count }>` — each via `apiFetch` + `unwrap`, `encodeURIComponent` on path segments, mirroring `listProjects`.

    Create `cloud/platform/src/pages/Connections.tsx` (`export function Connections({ orgId })`): fetch `listIntegrations`, render one row per provider with a connected/disconnected `Pill` and the workspace name; for Linear when disconnected, a "Connect Linear" button that calls `connectProvider` then `window.location.assign(authorizeUrl)` (server-side OAuth — the browser only ever receives the authorize URL, never the secret, T-20-07). Use the LoadState union + Eyebrow/Pill + formatApiError pattern from Projects.tsx. No component CSS file — globals.css tokens only.

    Create `cloud/platform/src/pages/ProjectIssues.tsx` (`export function ProjectIssues({ orgId, projectId })`): fetch `listIssues`, render rows with title, a state Pill (open/closed), and label chips via `JSON.parse(labels)`; a "Sync now" button calling `syncProject` then refetching (reflect updated count); a Linear Team picker shown when the project is Linear-eligible but unbound — selecting a team calls `selectLinearTeam` and then enables sync. Handle empty/disconnected/error states like Projects.tsx (e.g. 409 project_not_bound → prompt to pick a team or bind a repo).

    Wire into `cloud/platform/src/pages/OrgRoute.tsx` (REGISTRY — serialize, no parallel build): surface `Connections` (e.g. under the existing "settings" tab body or a dedicated connections surface) and route the project view to `ProjectIssues`. Document the chosen tab placement in the SUMMARY.
  </action>
  <verify>
    <automated>cd cloud/platform && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` in cloud/platform is clean.
    - `grep` confirms `listIssues`, `syncProject`, `connectProvider`, `selectLinearTeam`, `listIntegrations` exported from api.ts.
    - Connections.tsx and ProjectIssues.tsx exist, import Eyebrow/Pill from `@crawfish/ui`, and reference no new `.css` file (grep for a new css import returns nothing).
    - ProjectIssues parses `labels` with `JSON.parse` (grep) — honoring the JSON-as-string read contract.
    - OrgRoute.tsx references both new components (grep).
  </acceptance_criteria>
  <done>API helpers, Connections panel, and ProjectIssues view exist, wired into OrgRoute, reusing platform conventions and globals.css tokens; platform tsc clean.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>The Connections panel and Project Issues view in cloud/platform, wired to the Wave 2/3 routes. GitHub sync works end-to-end now; Linear works if the OAuth app from Plan 20-03 was registered.</what-built>
  <how-to-verify>
    1. Start the server and platform: `cd cloud/server && npm run dev` (or the documented start) and `cd cloud/platform && npm run dev` (Vite on 5174).
    2. Use the DEV-AUTH bypass (cloud/platform/DEV-AUTH.md): blank VITE_CLERK_PUBLISHABLE_KEY → dev@local user via X-User-Id shim. No real OAuth needed for the dev user.
    3. Open the platform, go to an org's connections surface — confirm GitHub shows connected (Clerk token present) and Linear shows connected/disconnected correctly.
    4. Open a project bound to a GitHub repo, click "Sync now" — confirm issue rows appear with state Pills and label chips, and the count reflects the synced issues. Confirm PRs do NOT appear (only issues).
    5. (If Linear app registered) On a project, pick a Linear Team, then "Sync now" — confirm ENG-### issues appear. (Else skip — note Linear deferred.)
    6. Confirm no token/secret strings appear anywhere in the rendered UI or network responses for GET /integrations (open devtools network).
  </how-to-verify>
  <resume-signal>Type "approved" if issues render with state/labels and Sync now updates counts, or describe what's wrong (e.g. labels not parsing, count stale, PRs leaking in).</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → API | The SPA reads issues/integrations and triggers connect/sync; all auth via apiFetch (dev shim or Clerk Bearer). |
| browser ← server (OAuth) | The browser receives ONLY the authorize URL from connect; it never sees client_secret or stored tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-20-07 | Information Disclosure | Connections panel / GET /integrations render | mitigate | The list endpoint returns only { provider, connected, externalWorkspaceName } (enforced server-side in Wave 3); the UI never requests or renders tokens. Connect redirects to the server-built authorizeUrl; the secret stays server-side. Checkpoint step 6 verifies no token strings in responses. |
| T-20-10 | Tampering (XSS via issue title/labels) | ProjectIssues rendering provider-supplied title/body/labels | mitigate | Render issue text as React text children (auto-escaped), not via dangerouslySetInnerHTML; parse labels with JSON.parse and render names as text. No raw HTML injection path. |
| T-20-03 | Elevation of Privilege | issues/sync/integrations calls scoped by orgId | mitigate | All helpers hit org-scoped routes; server enforces requireMember + 404 collapse (Wave 2/3). The UI cannot bypass server RBAC. |
| T-20-SC | Tampering | npm installs | mitigate | No new packages (RESEARCH audit: zero installs). slopcheck N/A. |
</threat_model>

<verification>
- `cd cloud/platform && npx tsc --noEmit -p tsconfig.json` clean.
- Human-verify checkpoint approved: issues render with state/labels, Sync now updates counts, no PRs leak in, no tokens in responses (DEV-AUTH walk).
- CSS only in ui/tokens/globals.css (no new component css file — grep).
</verification>

<success_criteria>
- The project page lists issues from GET …/issues with state/labels and a working "Sync now"; counts update after sync (success criterion 4).
- Issue state/title/labels/key/url round-trip from provider → DB → UI (success criterion 5).
- Connections panel shows per-provider state and a Connect Linear redirect; Linear sync requires a Team binding first.
- Platform tsc clean; verified via DEV-AUTH walk.
</success_criteria>

<output>
Create `.planning/phases/20-cloud-issue-ingestion-linear-github-connectors-with-postgres/20-04-SUMMARY.md` when done. Record the OrgRoute tab placement chosen for Connections and ProjectIssues, and whether Linear was exercised or deferred in the verification walk.
</output>
