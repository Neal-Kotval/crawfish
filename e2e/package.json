{
  "name": "crawfish-e2e",
  "version": "0.0.1",
  "private": true,
  "description": "Umbrella Playwright suite covering platform, dash, and crawfish-server bridge",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
